# /build — agent와 workflow 빌드

이 커맨드의 스크립트 위치: `<plugin-root>/skills/build/scripts/build-dynamic.sh`

인자를 그대로 전달하여 실행한다.

- 인자 없이: 전체 빌드 (agent → workflow)
- `--agent`: agent만
- `--workflow`: workflow만
